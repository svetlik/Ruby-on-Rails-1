<h1>Create a new post</h1>

<%= form_for @post do |p| %>
  <p>
    <%= p.label :title %>
    <%= p.text_field :title %>
  </p>
  <p>
    <%= p.label :body %>
    <%= p.text_field :body %>
  </p>
  <p>
    <%= p.submit %>
  </p>
<% end %>

<% link_to 'Back to posts', posts_path %>